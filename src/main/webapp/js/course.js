/*let add=()=>{
	if(courseName.value!="" && instructorName.value!="" && price.value!="" &&
		category.value!="" && description.value!="" && startDate.value!="" && period.value!="" )
	message.innerHTML= "Adding new course is successful!"
	//event.preventDefault()
	
}


let allclear=()=>{
	courseName.value=""
	instructorName.value=""
	price.value=""
	category.value=""
	description.value=""
	startDate.value=""
	period.value=""
	message.innerHTML= ""
}*/

let allclear=()=>{
	message.innerHTML=""
}